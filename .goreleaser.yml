# GoReleaser configuration for Daryaft.
# Public installable release channels must not be published before v1.0.0.

project_name: daryaft

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: daryaft
    main: .
    binary: daryaft
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/he8um/daryaft/pkg/version.Version={{.Version}}
      - -X github.com/he8um/daryaft/pkg/version.Commit={{.Commit}}
      - -X github.com/he8um/daryaft/pkg/version.Date={{.Date}}
      - -X github.com/he8um/daryaft/pkg/version.BuiltBy=goreleaser

archives:
  - id: default
    builds:
      - daryaft
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# Package publishing placeholders for v1.0.0 and later.
# Enable these sections only when public install channels are official.
#
# nfpms:
#   - id: packages
#     package_name: daryaft
#     vendor: AmirHesam Piri
#     homepage: https://xhesam.com/daryaft
#     maintainer: AmirHesam Piri <info@xhesam.com>
#     description: Modern terminal downloader written in Go.
#     license: MIT
#     formats:
#       - deb
#       - rpm
#       - archlinux
#
# brews:
#   - name: daryaft
#     repository:
#       owner: he8um
#       name: homebrew-tap
#     homepage: https://xhesam.com/daryaft
#     description: Modern terminal downloader written in Go.
#     license: MIT

# Homebrew, deb, rpm, and arch package publishing should be enabled only from v1.0.0 onward.
# See docs/roadmap/release-train.md and docs/engineering/release-process.md.
